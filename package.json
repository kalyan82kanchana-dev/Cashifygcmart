{
  "name": "cashifygcmart",
  "version": "1.0.0",
  "description": "Cashifygcmart - Gift Card Exchange Platform",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && python3 -m uvicorn server:app --host 0.0.0.0 --port $PORT"
  }
}
